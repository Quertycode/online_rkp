# 🚀 Быстрый старт - База заданий

## Для пользователя

### Что нужно сделать:

1. **Установить Python 3.7+** (если еще не установлен)
   - Скачать с [python.org](https://www.python.org/downloads/)
   - При установке поставить галочку "Add Python to PATH"

2. **Установить зависимости**

```bash
cd server/scripts
pip install requests beautifulsoup4
```

3. **Запустить загрузку тестовых данных**

**Windows:**
```bash
cd server/scripts
load_test_data.bat
```

**Linux/Mac:**
```bash
cd server/scripts
chmod +x load_test_data.sh
./load_test_data.sh
```

Это загрузит:
- ✅ Математика (База) ОГЭ - 30 заданий
- ✅ Биология ОГЭ - 30 заданий

4. **Запустить сервер**

```bash
cd server
npm run start:dev
```

5. **Открыть приложение**

Перейти на `/tasks` и проверить отображение заданий с изображениями!

---

## Для разработчика

### 📁 Структура проекта

```
EduMVP/
├── sdamgia-api/                 # Python API для СДАМ ГИА
│   └── sdamgia/
│       └── __init__.py
├── server/
│   ├── tasksbd.db              # SQLite база данных
│   ├── image_tasksdb/          # Изображения к заданиям
│   │   ├── mathb/
│   │   └── bio/
│   ├── scripts/
│   │   ├── load_tasks.py       # Скрипт загрузки заданий
│   │   ├── clean_db.py         # Скрипт очистки БД
│   │   ├── load_test_data.bat  # Автозагрузка тестовых данных (Windows)
│   │   ├── load_test_data.sh   # Автозагрузка тестовых данных (Linux/Mac)
│   │   └── README.md           # Подробная документация
│   └── src/modules/tasks/
│       ├── tasks.controller.ts # API эндпоинты
│       └── tasks.service.ts    # Логика работы с БД
└── src/
    ├── pages/Tasks.jsx         # Страница банка заданий
    └── modules/tasks/
        └── components/
            ├── TaskItem.jsx    # Компонент задания
            └── TaskList.jsx    # Список заданий
```

### 🔧 API эндпоинты

**Задания:**
- `GET /tasks` - получить все задания (с фильтрами)
- `GET /tasks/:id` - получить задание по ID
- `GET /tasks/subject/:subject` - задания по предмету

**Метаданные:**
- `GET /tasks/meta/subjects` - список предметов
- `GET /tasks/meta/topics` - список тем
- `GET /tasks/meta/categories` - список категорий
- `GET /tasks/meta/lines` - список линий (номеров заданий)

**Изображения:**
- `GET /tasks/images/:path` - получить изображение

### 🎯 Как работает система

1. **Загрузка данных** (`load_tasks.py`)
   - Обращается к API СДАМ ГИА
   - Скачивает задания и изображения
   - Сохраняет в SQLite БД
   - Порядок изображений сохраняется через `image_order`

2. **Backend** (NestJS)
   - Читает данные из SQLite
   - Форматирует текст с markdown изображениями
   - Отдает изображения через `/tasks/images/*`

3. **Frontend** (React)
   - Парсит markdown с изображениями
   - Отображает задания с изображениями в правильном порядке
   - Фильтры по предмету, теме, линии, источнику

### 📊 Структура БД

Основные таблицы:
- `subjects` - предметы (с типом экзамена ОГЭ/ЕГЭ)
- `topics` - темы/задания
- `categories` - категории
- `problems` - задачи (условие, решение, ответ)
- `problem_condition_images` - изображения условия (**с порядком!**)
- `problem_solution_images` - изображения решения (**с порядком!**)

### ⚙️ Доступные предметы

| Код | Предмет | Код | Предмет |
|-----|---------|-----|---------|
| `mathb` | Математика (База) | `math` | Математика (Профиль) |
| `bio` | Биология | `rus` | Русский язык |
| `phys` | Физика | `chem` | Химия |
| `inf` | Информатика | `geo` | География |
| `soc` | Обществознание | `hist` | История |
| `lit` | Литература | `en` | Английский язык |
| `de` | Немецкий язык | `fr` | Французский язык |
| `sp` | Испанский язык | | |

### 🔄 Рабочий процесс

#### Первоначальная загрузка

```bash
# 1. Очистить старые данные
cd server/scripts
python clean_db.py --confirm

# 2. Загрузить тестовые данные
python load_tasks.py --subject mathb --exam-type oge --count 30
python load_tasks.py --subject bio --exam-type oge --count 30

# 3. Запустить сервер
cd ..
npm run start:dev

# 4. Проверить в браузере /tasks
```

#### Добавление новых заданий

```bash
# Добавить еще 20 заданий по математике
python load_tasks.py --subject mathb --exam-type oge --count 20

# Добавить задания по физике
python load_tasks.py --subject phys --exam-type oge --count 30
```

#### Обновление данных по предмету

```bash
# Удалить данные по предмету
python clean_db.py --confirm --subject mathb

# Загрузить новые данные
python load_tasks.py --subject mathb --exam-type oge --count 50
```

### 🐛 Troubleshooting

**Python не найден:**
- Установите Python 3.7+
- Добавьте Python в PATH

**Ошибка импорта sdamgia:**
- Проверьте структуру папок (sdamgia-api должен быть на уровне EduMVP)

**База данных заблокирована:**
- Остановите backend сервер перед запуском скриптов

**Изображения не отображаются:**
- Проверьте, что backend запущен
- Проверьте консоль браузера на ошибки
- Проверьте, что путь к изображениям правильный

### 📝 TODO

- [ ] Добавить кэширование каталогов
- [ ] Добавить прогресс-бар для загрузки
- [ ] Оптимизировать загрузку больших объемов данных
- [ ] Добавить валидацию скачанных данных
- [ ] Добавить тесты для скриптов


