# 📚 База заданий - Система загрузки и отображения

## ✅ Что сделано

### 1. ✅ Python скрипты для загрузки данных

**Создано:**
- `server/scripts/load_tasks.py` - универсальный скрипт загрузки заданий
- `server/scripts/clean_db.py` - скрипт очистки БД
- `server/scripts/load_test_data.bat` - автозагрузка тестовых данных (Windows)
- `server/scripts/load_test_data.sh` - автозагрузка тестовых данных (Linux/Mac)
- `server/scripts/clean_all.bat` - очистка всех данных (Windows)
- `server/scripts/clean_all.sh` - очистка всех данных (Linux/Mac)

**Возможности:**
- ✅ Загрузка заданий по любому предмету (mathb, bio, rus, phys и т.д.)
- ✅ Поддержка ОГЭ и ЕГЭ
- ✅ Автоматическое скачивание изображений
- ✅ Сохранение порядка изображений
- ✅ Проверка дубликатов
- ✅ Задержка между запросами (0.5 сек)
- ✅ Подробное логирование процесса

### 2. ✅ Backend API готов к работе

**Эндпоинты:**
- `GET /tasks` - получить задания (с фильтрами)
- `GET /tasks/:id` - получить задание по ID
- `GET /tasks/subject/:subject` - задания по предмету
- `GET /tasks/meta/subjects` - список предметов
- `GET /tasks/meta/topics` - список тем
- `GET /tasks/meta/categories` - список категорий
- `GET /tasks/meta/lines` - список линий заданий
- `GET /tasks/images/*` - отдача изображений

**Особенности:**
- ✅ Работа с SQLite БД через better-sqlite3
- ✅ Автоматическая вставка изображений в markdown
- ✅ Сохранение порядка изображений (image_order)
- ✅ Каскадное удаление данных
- ✅ Поддержка фильтров по предмету, теме, категории, линии
- ✅ Разделение ОГЭ/ЕГЭ

### 3. ✅ Frontend готов к отображению

**Компоненты:**
- `src/pages/Tasks.jsx` - страница банка заданий
- `src/modules/tasks/components/TaskItem.jsx` - карточка задания
- `src/modules/tasks/components/TaskList.jsx` - список заданий
- `src/modules/tasks/components/FiltersBar.jsx` - панель фильтров

**Возможности:**
- ✅ Парсинг markdown изображений `![alt](url)`
- ✅ Автоматическая конвертация путей к API
- ✅ Отображение изображений в правильном порядке
- ✅ Фильтры по предмету, теме, линии
- ✅ Поиск по тексту задания
- ✅ Отображение решений с изображениями
- ✅ Адаптивный дизайн

### 4. ✅ База данных

**Структура:**
```sql
subjects              -- Предметы (код, название, тип экзамена)
├── topics            -- Темы (номер, название, линия)
│   └── categories    -- Категории заданий
└── problems          -- Задачи (условие, решение, ответ)
    ├── problem_condition_images   -- Изображения условия (с порядком!)
    └── problem_solution_images    -- Изображения решения (с порядком!)
```

**Особенности:**
- ✅ Поле `image_order` для сохранения порядка изображений
- ✅ Каскадное удаление (FK constraints)
- ✅ Индексы для быстрого поиска
- ✅ Автоматические триггеры для updated_at
- ✅ Поддержка ОГЭ/ЕГЭ через exam_type

### 5. ✅ Документация

**Создано:**
- `server/scripts/README.md` - подробная документация по скриптам
- `server/scripts/QUICKSTART.md` - быстрый старт для разработчиков
- `server/scripts/SYSTEM_OVERVIEW.md` - обзор системы (этот файл)

---

## 🎯 Как использовать

### Шаг 1: Установить зависимости

```bash
cd server/scripts
pip install requests beautifulsoup4
```

### Шаг 2: Загрузить тестовые данные

**Windows:**
```bash
load_test_data.bat
```

**Linux/Mac:**
```bash
chmod +x load_test_data.sh
./load_test_data.sh
```

Это загрузит:
- Математика (База) ОГЭ - 30 заданий
- Биология ОГЭ - 30 заданий

### Шаг 3: Запустить backend

```bash
cd ..
npm run start:dev
```

### Шаг 4: Открыть приложение

Перейти на `/tasks` - должны отображаться задания с изображениями!

---

## 📝 Дальнейшая работа

### Загрузить больше данных

```bash
cd server/scripts

# Русский язык ОГЭ
python load_tasks.py --subject rus --exam-type oge --count 30

# Физика ОГЭ
python load_tasks.py --subject phys --exam-type oge --count 30

# Математика Профиль ЕГЭ
python load_tasks.py --subject math --exam-type ege --count 50
```

### Очистить данные

```bash
# Удалить все данные
python clean_db.py --confirm

# Удалить данные по одному предмету
python clean_db.py --confirm --subject mathb
```

---

## 🔍 Проверка работы

### 1. Проверить БД

```bash
cd server
sqlite3 tasksbd.db "SELECT COUNT(*) FROM problems;"
sqlite3 tasksbd.db "SELECT COUNT(*) FROM problem_condition_images;"
sqlite3 tasksbd.db "SELECT COUNT(*) FROM problem_solution_images;"
```

### 2. Проверить изображения

```bash
ls -la image_tasksdb/mathb/
ls -la image_tasksdb/bio/
```

### 3. Проверить API

```bash
curl http://localhost:3001/tasks/meta/subjects
curl http://localhost:3001/tasks?subject=mathb
```

### 4. Проверить frontend

Открыть `/tasks` в браузере и:
- ✅ Видны задания
- ✅ Изображения загружаются
- ✅ Изображения в правильном порядке
- ✅ Фильтры работают
- ✅ Решения отображаются с изображениями

---

## 🎨 Архитектура системы

```
┌─────────────────┐
│   СДАМ ГИА API  │  (sdamgia-api)
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────┐
│  Python загрузчик (load_tasks)  │
│  - Скачивает задания            │
│  - Скачивает изображения        │
│  - Сохраняет в БД               │
└────────┬────────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│  SQLite БД (tasksbd.db)              │
│  + Изображения (image_tasksdb/)      │
│                                      │
│  Таблицы:                            │
│  - subjects (предметы)               │
│  - topics (темы)                     │
│  - categories (категории)            │
│  - problems (задачи)                 │
│  - problem_*_images (изображения)    │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  NestJS Backend API              │
│  - Читает БД                     │
│  - Форматирует markdown          │
│  - Отдает изображения            │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  React Frontend                  │
│  - Парсит markdown               │
│  - Отображает задания            │
│  - Фильтры, поиск                │
└──────────────────────────────────┘
```

---

## 📊 Поддерживаемые предметы

### ОГЭ (9 класс)
- Математика (База) - `mathb`
- Русский язык - `rus`
- Биология - `bio`
- Физика - `phys`
- Химия - `chem`
- Информатика - `inf`
- География - `geo`
- Обществознание - `soc`
- История - `hist`
- Литература - `lit`
- Английский язык - `en`
- Немецкий язык - `de`
- Французский язык - `fr`
- Испанский язык - `sp`

### ЕГЭ (11 класс)
Все те же предметы + Математика (Профиль) - `math`

---

## ⚡ Особенности реализации

### Порядок изображений

**Проблема:** Изображения должны отображаться в правильном порядке (как в условии, так и в решении).

**Решение:**
1. В БД хранится поле `image_order` (0, 1, 2, ...)
2. Backend сортирует изображения по `image_order`
3. Backend вставляет markdown в текст: `![Изображение 1](url)`
4. Frontend парсит markdown и отображает в правильном порядке

### Разделение ОГЭ/ЕГЭ

**Проблема:** Биология ОГЭ и Биология ЕГЭ - разные предметы.

**Решение:**
1. В таблице `subjects` есть поле `exam_type` (oge/ege)
2. При загрузке указывается `--exam-type oge` или `--exam-type ege`
3. Frontend фильтрует по типу экзамена на основе класса пользователя

### Изображения

**Проблема:** Изображения нужно хранить локально.

**Решение:**
1. Скрипт скачивает изображения в `image_tasksdb/{subject}/{problem_id}/`
2. В БД хранится относительный путь: `image_tasksdb/mathb/26863/condition_0.png`
3. Backend отдает изображения через `/tasks/images/*`
4. Frontend конвертирует пути к API: `http://localhost:3001/tasks/images/mathb/26863/condition_0.png`

### Фильтрация

**Фильтры:**
- Предмет (`subject`)
- Тема (`topic`)
- Категория (`category`)
- Линия (`line` - номер задания в ОГЭ/ЕГЭ)
- Источник (`source` - всегда "sdamgia" пока)
- Поиск по тексту

**Реализация:**
- Backend: SQL запросы с WHERE условиями
- Frontend: `useMemo` для фильтрации в реальном времени

---

## 🐛 Известные проблемы

1. **Python не в PATH** - нужно добавить Python в PATH при установке
2. **База занята** - нужно останавливать backend перед запуском скриптов
3. **Медленная загрузка** - задержка 0.5 сек между запросами к СДАМ ГИА (можно уменьшить, но рискованно)

---

## 🚀 Будущие улучшения

- [ ] Кэширование каталогов СДАМ ГИА
- [ ] Прогресс-бар для загрузки
- [ ] Параллельная загрузка изображений
- [ ] Валидация скачанных данных
- [ ] Автоматическое обновление данных
- [ ] Экспорт заданий в PDF
- [ ] Импорт заданий из других источников
- [ ] Тесты для скриптов
- [ ] Docker контейнер для изоляции
- [ ] Web интерфейс для управления загрузкой

---

## 📞 Поддержка

При возникновении проблем:
1. Проверить логи скриптов
2. Проверить логи backend сервера
3. Проверить консоль браузера
4. Посмотреть документацию в `README.md` и `QUICKSTART.md`

---

## 📄 Лицензия

Используется API СДАМ ГИА - убедитесь, что соблюдаете правила использования сайта.

---

**Создано:** 2025-11-22  
**Версия:** 1.0.0  
**Автор:** AI Assistant + querty


