# 📚 База заданий - Загрузка данных из СДАМ ГИА

## 🎯 Что это?

Система для автоматической загрузки заданий из сайта СДАМ ГИА в локальную базу данных с последующим отображением в банке заданий EduMVP.

**Возможности:**
- ✅ Загрузка заданий по любому предмету (математика, биология, физика и т.д.)
- ✅ Поддержка ОГЭ и ЕГЭ
- ✅ Автоматическое скачивание изображений
- ✅ **Сохранение порядка изображений**
- ✅ Фильтрация по предмету, теме, линии, категории
- ✅ Отображение решений с изображениями
- ✅ Поиск по тексту задания

---

## 🚀 Быстрый старт

### 1. Установить Python зависимости

```bash
cd server/scripts
pip install requests beautifulsoup4
```

### 2. Загрузить тестовые данные

**Windows:**
```bash
load_test_data.bat
```

**Linux/Mac:**
```bash
chmod +x load_test_data.sh
./load_test_data.sh
```

Это загрузит 60 заданий:
- Математика (База) ОГЭ - 30 заданий
- Биология ОГЭ - 30 заданий

### 3. Запустить backend

```bash
cd ..
npm run start:dev
```

### 4. Открыть приложение

Перейти на `/tasks` - задания с изображениями готовы! 🎉

---

## 📁 Структура

```
server/
├── tasksbd.db              # SQLite база данных
├── image_tasksdb/          # Изображения к заданиям
│   ├── mathb/              # Математика База
│   └── bio/                # Биология
└── scripts/                # Python скрипты
    ├── load_tasks.py       # Основной скрипт загрузки
    ├── clean_db.py         # Очистка БД
    ├── load_test_data.bat  # Автозагрузка (Windows)
    ├── load_test_data.sh   # Автозагрузка (Linux/Mac)
    ├── README.md           # Подробная документация
    ├── QUICKSTART.md       # Быстрый старт для разработчиков
    └── SYSTEM_OVERVIEW.md  # Обзор системы
```

---

## 📖 Документация

- **[README.md](./scripts/README.md)** - подробная документация по скриптам
- **[QUICKSTART.md](./scripts/QUICKSTART.md)** - быстрый старт для разработчиков
- **[SYSTEM_OVERVIEW.md](./scripts/SYSTEM_OVERVIEW.md)** - полный обзор системы

---

## 🎓 Доступные предметы

| Предмет | Код | Предмет | Код |
|---------|-----|---------|-----|
| Математика (База) | `mathb` | Математика (Профиль) | `math` |
| Биология | `bio` | Русский язык | `rus` |
| Физика | `phys` | Химия | `chem` |
| Информатика | `inf` | География | `geo` |
| Обществознание | `soc` | История | `hist` |
| Литература | `lit` | Английский язык | `en` |
| Немецкий язык | `de` | Французский язык | `fr` |
| Испанский язык | `sp` | | |

---

## 💡 Примеры использования

### Загрузить задания по физике

```bash
cd server/scripts
python load_tasks.py --subject phys --exam-type oge --count 30
```

### Загрузить задания по русскому языку (ЕГЭ)

```bash
python load_tasks.py --subject rus --exam-type ege --count 50
```

### Добавить еще заданий к существующим

```bash
# Скрипт автоматически пропускает дубликаты
python load_tasks.py --subject mathb --exam-type oge --count 50
```

### Очистить данные и загрузить заново

```bash
# Очистить только математику
python clean_db.py --confirm --subject mathb

# Загрузить заново
python load_tasks.py --subject mathb --exam-type oge --count 30
```

---

## 🔍 Как это работает?

### 1. Загрузка данных

```
Python скрипт → API СДАМ ГИА → SQLite БД + Изображения
```

Скрипт `load_tasks.py`:
1. Получает каталог заданий с СДАМ ГИА
2. Для каждой категории загружает задания
3. Скачивает изображения условия и решения
4. Сохраняет в БД с правильным порядком изображений

### 2. Backend API

```
NestJS → SQLite БД → Форматирование markdown → JSON API
```

Backend (`tasks.service.ts`):
1. Читает данные из БД
2. Загружает изображения с правильным порядком
3. Вставляет markdown `![](url)` в текст
4. Отдает через REST API

### 3. Frontend

```
React → Парсинг markdown → Отображение заданий с изображениями
```

Frontend (`TaskItem.jsx`):
1. Получает задания через API
2. Парсит markdown изображения
3. Отображает текст и изображения в правильном порядке
4. Фильтрует по предмету, теме, линии

---

## 🎨 Особенности

### ✨ Порядок изображений

Изображения отображаются **строго в том порядке**, в котором они были на СДАМ ГИА:

1. В БД хранится поле `image_order` (0, 1, 2, ...)
2. Backend сортирует по этому полю
3. Frontend отображает в правильном порядке

### 🔀 Разделение ОГЭ/ЕГЭ

Биология ОГЭ и Биология ЕГЭ - **разные предметы**:

1. В БД поле `exam_type` (oge/ege)
2. При загрузке указывается тип экзамена
3. Frontend фильтрует по классу пользователя

### 📂 Организация изображений

```
image_tasksdb/
├── mathb/
│   ├── 26863/
│   │   └── condition_0.png
│   └── 26864/
│       ├── condition_0.png
│       └── solution_0.svg
└── bio/
    └── 4612/
        ├── condition_0.jpg
        └── solution_0.png
```

---

## 🐛 Troubleshooting

### Python не найден
```bash
# Установить Python 3.7+ с python.org
# При установке поставить галочку "Add Python to PATH"
```

### База данных заблокирована
```bash
# Остановить backend сервер перед запуском скриптов
cd server
# Ctrl+C для остановки
```

### Изображения не отображаются
```bash
# 1. Проверить, что backend запущен
# 2. Открыть консоль браузера (F12)
# 3. Проверить ошибки загрузки изображений
# 4. Проверить путь: http://localhost:3001/tasks/images/mathb/26863/condition_0.png
```

---

## 📊 API эндпоинты

### Задания
- `GET /tasks` - все задания (с фильтрами)
- `GET /tasks/:id` - задание по ID
- `GET /tasks/subject/:subject` - задания по предмету

### Метаданные
- `GET /tasks/meta/subjects` - список предметов
- `GET /tasks/meta/topics` - список тем
- `GET /tasks/meta/categories` - список категорий
- `GET /tasks/meta/lines` - список линий (номеров заданий)

### Изображения
- `GET /tasks/images/:path` - изображение

**Пример:**
```bash
curl http://localhost:3001/tasks?subject=mathb&topicLine=1
curl http://localhost:3001/tasks/images/mathb/26863/condition_0.png
```

---

## 🚀 Дальнейшие улучшения

- [ ] Web интерфейс для управления загрузкой
- [ ] Автоматическое обновление данных (cron job)
- [ ] Экспорт заданий в PDF
- [ ] Импорт из других источников
- [ ] Кэширование каталогов
- [ ] Прогресс-бар загрузки
- [ ] Docker контейнер

---

## 📞 Помощь

Документация:
1. **[README.md](./scripts/README.md)** - подробные инструкции
2. **[QUICKSTART.md](./scripts/QUICKSTART.md)** - быстрый старт
3. **[SYSTEM_OVERVIEW.md](./scripts/SYSTEM_OVERVIEW.md)** - архитектура системы

При проблемах:
1. Проверить логи скриптов
2. Проверить логи backend (терминал)
3. Проверить консоль браузера (F12)

---

## 📄 Использование

Система использует API СДАМ ГИА для образовательных целей. Убедитесь, что соблюдаете правила использования сайта.

---

**Готово к использованию! 🎉**


