# НАЙДЕНЫ ПРОБЛЕМЫ И РЕШЕНИЯ

## 🐛 Проблема 1: PNG файлы не открываются

**Причина:** СДАМ ГИА отдает SVG файлы, но скрипт сохраня их как PNG

**Решение:** ✅ Исправлен скрипт `load_tasks.py` - теперь определяет тип файла по содержимому

## 🐛 Проблема 2: Backend не запущен

**Причина:** ERR_CONNECTION_REFUSED - backend не запущен на порту 3001

**Решение:** Нужно запустить backend

## 🐛 Проблема 3: Переносы строк и порядок изображений

**Причина:** Изображения добавлялись в конец текста, переносы строк не сохранялись

**Решение:** ✅ Исправлен `tasks.service.ts` - изображения теперь вставляются в текст, переносы строк сохраняются

---

## 🔧 ЧТО НУЖНО СДЕЛАТЬ

### 1. Очистить БД и изображения

```bash
cd server/scripts
node init-db.js
```

### 2. Перезагрузить данные с исправленным скриптом

```bash
py load_tasks.py --subject mathb --exam-type oge --count 30
py load_tasks.py --subject bio --exam-type oge --count 30
```

### 3. Запустить backend

```bash
cd server
npm run start:dev
```

### 4. Проверить в браузере `/tasks`

---

## ✅ Что исправлено

1. **Определение типа файла** - по содержимому (SVG/PNG/JPG)
2. **Сохранение переносов строк** - `\n` сохраняются в тексте
3. **Порядок изображений** - вставляются в правильном порядке
4. **Вставка в контекст** - изображения в тексте, а не в конце

---

**Запустите команды выше чтобы все исправить!**


