# 📊 Структура данных, загружаемых с сайта СДАМ ГИА

## Что загружается с сайта СДАМ ГИА?

### 1. 📚 **Каталог заданий (Catalog)**
Полная структура предмета: задания → категории

**Метод API:** `sdamgia.get_catalog(subject)`

**Пример данных:**
```python
[
  {
    'topic_id': '1',
    'topic_name': 'Простейшие текстовые задачи',
    'categories': [
      {'category_id': '174', 'category_name': 'Вычисления'},
      {'category_id': '1', 'category_name': 'Округление с недостатком'},
      {'category_id': '2', 'category_name': 'Округление с избытком'},
      {'category_id': '249', 'category_name': 'Проценты'}
    ]
  },
  {
    'topic_id': '2',
    'topic_name': 'Чтение графиков и диаграмм',
    'categories': [...]
  }
]
```

**Что сохраняется в БД:**
- ✅ Предмет (`subjects`)
- ✅ Задания (`topics`) - номер и название
- ✅ Категории (`categories`) - ID и название

---

### 2. 📝 **Задача (Problem)**
Полная информация о конкретной задаче

**Метод API:** `sdamgia.get_problem_by_id(subject, problem_id)`

**Пример данных:**
```python
{
  'id': '1001',                    # ID задачи из СДАМ ГИА
  'topic': '4',                     # Номер задания
  'condition': {
    'text': 'На экзамен вынесено 60 вопросов...',  # Текст условия
    'images': [                     # URL изображений из условия
      'https://math-ege.sdamgia.ru/formula/svg/9f/9fbf55ab44a507fb47ba8a2666cd7644.svg'
    ]
  },
  'solution': {
    'text': 'Решение. Андрей выучил 60 – 3 = 57 вопросов...',  # Текст решения
    'images': [                    # URL изображений из решения
      'https://math-ege.sdamgia.ru/formula/svg/9f/9fbf55ab44a507fb47ba8a2666cd7644.svg'
    ]
  },
  'answer': '0,95',                # Ответ на задачу
  'analogs': [                     # Список ID аналогичных задач
    '1001', '1002', '1003', '1004', '1005'
  ],
  'url': 'https://math-ege.sdamgia.ru/problem?id=1001'  # URL задачи на сайте
}
```

**Что сохраняется в БД:**
- ✅ Основная информация (`problems`):
  - ID задачи
  - Текст условия (с встроенными ссылками на локальные изображения)
  - Текст решения (с встроенными ссылками на локальные изображения)
  - Ответ
  - URL задачи
  - Номер задания (topic)
  - **Линия заданий (line)** - к какой линии относится задача
- ✅ Изображения условий (`problem_condition_images`):
  - Оригинальный URL изображения
  - **Локальный путь к изображению** (сохранено в `image_tasksdb/`)
  - Порядок изображений
- ✅ Изображения решений (`problem_solution_images`):
  - Оригинальный URL изображения
  - **Локальный путь к изображению** (сохранено в `image_tasksdb/`)
  - Порядок изображений
- ✅ Аналогичные задачи (`problem_analogs`):
  - Связи с другими задачами

---

### 3. 🔍 **Поиск задач (Search)**
Поиск задач по текстовому запросу

**Метод API:** `sdamgia.search(subject, request, page=1)`

**Пример данных:**
```python
['6407', '8795', '8799', '27501', '519508', '519534', ...]  # Список ID найденных задач
```

**Что сохраняется в БД:**
- ✅ Используется для получения списка ID задач
- ✅ Затем каждая задача загружается через `get_problem_by_id()`

---

### 4. 📋 **Тест (Test)**
Информация о тесте и задачах в нем

**Метод API:** `sdamgia.get_test_by_id(subject, test_id)`

**Пример данных:**
```python
['77345', '28765', '77374', '27903', '26675', '27700', ...]  # Список ID задач в тесте
```

**Что сохраняется в БД:**
- ✅ Информация о тесте (`tests`):
  - ID теста из СДАМ ГИА
  - Предмет
- ✅ Связь теста с задачами (`test_problems`):
  - Какие задачи входят в тест
  - Порядок задач в тесте

---

### 5. 🏷️ **Категория (Category)**
Список задач в категории

**Метод API:** `sdamgia.get_category_by_id(subject, category_id, page=1)`

**Пример данных:**
```python
['77334', '323512', '501201', '509077', '509106']  # Список ID задач в категории
```

**Что сохраняется в БД:**
- ✅ Связь категории с задачами (`category_problems`):
  - Какие задачи относятся к категории

---

### 6. 🎲 **Генерация теста (Generate Test)**
Создание нового теста с заданными параметрами

**Метод API:** `sdamgia.generate_test(subject, problems)`

**Пример данных:**
```python
38299510  # ID сгенерированного теста
```

**Что сохраняется в БД:**
- ✅ После генерации тест загружается через `get_test_by_id()`

---

### 7. 📄 **PDF теста (Generate PDF)**
Генерация PDF версии теста

**Метод API:** `sdamgia.generate_pdf(subject, test_id, ...)`

**Пример данных:**
```python
'https://math-ege.sdamgia.ru/pdf/1fe7d7d8408f8d5195fabfd8ab393d63.pdf'  # URL PDF файла
```

**Что сохраняется в БД:**
- ⚠️ PDF файлы не сохраняются в БД (только URL, если нужно)

---

## 📦 Полная структура данных в БД

```
Предмет (subjects)
├── Код предмета (math, rus, bio...)
├── Название (Математика, Русский язык...)
└── Тип экзамена (oge, ege)

Задание (topics)
├── Номер задания (1, Д1, C4...)
├── Название задания
└── Линия заданий (если есть)

Категория (categories)
├── ID категории из СДАМ ГИА
└── Название категории

Задача (problems) ⭐ ОСНОВНАЯ ТАБЛИЦА
├── ID задачи из СДАМ ГИА
├── Линия заданий (line) - к какой линии относится задача
├── Текст условия (condition_text) - включает ссылки на локальные изображения
├── Текст решения (solution_text) - включает ссылки на локальные изображения
├── Ответ (answer)
├── URL задачи
└── Источник данных (sdamgia)

Изображения условий (problem_condition_images)
├── Оригинальный URL изображения
├── Локальный путь к изображению (image_tasksdb/{subject}/{problem_id}/condition_0.png)
└── Порядок в условии

Изображения решений (problem_solution_images)
├── Оригинальный URL изображения
├── Локальный путь к изображению (image_tasksdb/{subject}/{problem_id}/solution_0.png)
└── Порядок в решении

Аналогичные задачи (problem_analogs)
└── Связи с другими задачами

Тест (tests)
├── ID теста из СДАМ ГИА
└── Связь с задачами (test_problems)
```

## 🎯 Что НЕ загружается

- ❌ PDF файлы (только URL, если нужно)
- ❌ Пользовательские данные (решения пользователей, статистика и т.д.)

## 📁 Хранение изображений

**Изображения скачиваются и сохраняются локально:**
- Папка: `C:\Users\querty\source\repos\EduMVP\server\image_tasksdb`
- Структура: `image_tasksdb/{subject}/{problem_id}/condition_0.png`, `solution_0.png` и т.д.
- В БД сохраняются пути к локальным файлам
- В текст задачи и решения автоматически добавляются ссылки на изображения в формате Markdown: `![Изображение 1](path/to/image.png)`

## 📊 Пример загрузки данных

```typescript
// 1. Загружаем каталог (структуру предмета)
await importCatalog(db, sdamgia, 'math', 'Математика', 'oge')

// 2. Загружаем конкретную задачу
await importProblem(db, sdamgia, 'math', 'Математика', '1001', 'oge')

// 3. Ищем задачи по запросу
const foundIds = sdamgia.search('math', 'Найдите количество')
for (const id of foundIds) {
  await importProblem(db, sdamgia, 'math', 'Математика', id, 'oge')
}

// 4. Загружаем тест
const testId = sdamgia.generate_test('math', {1: 1, 2: 2, 3: 4})
const problemIds = sdamgia.get_test_by_id('math', testId)
for (const id of problemIds) {
  await importProblem(db, sdamgia, 'math', 'Математика', id, 'oge')
}
```

## 🔄 Обновление данных

- При повторной загрузке задачи она **обновляется** (не дублируется)
- Изображения **перезаписываются**
- Аналогичные задачи **обновляются**

