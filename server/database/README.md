# 📚 База данных банка заданий СДАМ ГИА

## Описание

База данных SQLite для хранения заданий с сайта СДАМ ГИА. Поддерживает полную структуру данных из API, включая условия, решения, изображения, категории и тесты.

## Структура базы данных

### Основные таблицы

#### 1. `subjects` - Предметы
Хранит информацию о предметах (математика, русский язык и т.д.)

**Поля:**
- `id` - Уникальный идентификатор
- `code` - Код предмета (math, rus, bio и т.д.)
- `name` - Название предмета
- `exam_type` - Тип экзамена: 'oge' или 'ege'
- `created_at` - Дата создания
- `updated_at` - Дата обновления

**Пример:**
```sql
INSERT INTO subjects (code, name, exam_type) 
VALUES ('math', 'Математика', 'oge');
```

#### 2. `topics` - Задания
Хранит информацию о заданиях (номер задания, название)

**Поля:**
- `id` - Уникальный идентификатор
- `subject_id` - Связь с предметом
- `topic_number` - Номер задания ("1", "Д1", "C4")
- `topic_name` - Название задания
- `topic_line` - Линия заданий (если есть)
- `created_at`, `updated_at` - Даты

**Пример:**
```sql
INSERT INTO topics (subject_id, topic_number, topic_name) 
VALUES (1, '1', 'Простейшие текстовые задачи');
```

#### 3. `categories` - Категории
Хранит информацию о категориях внутри заданий

**Поля:**
- `id` - Уникальный идентификатор
- `topic_id` - Связь с заданием
- `category_id` - ID категории из СДАМ ГИА
- `category_name` - Название категории
- `created_at`, `updated_at` - Даты

**Пример:**
```sql
INSERT INTO categories (topic_id, category_id, category_name) 
VALUES (1, '174', 'Вычисления');
```

#### 4. `problems` - Задачи
Основная таблица с задачами

**Поля:**
- `id` - Уникальный идентификатор
- `subject_id` - Связь с предметом
- `topic_id` - Связь с заданием (может быть NULL)
- `problem_id` - ID задачи из СДАМ ГИА
- `line` - Линия заданий (например, "1", "2", "3")
- `condition_text` - Текст условия задачи (включает ссылки на локальные изображения)
- `solution_text` - Текст решения задачи (включает ссылки на локальные изображения)
- `answer` - Ответ на задачу
- `url` - URL задачи на сайте СДАМ ГИА
- `source` - Источник данных (sdamgia, manual и т.д.)
- `created_at`, `updated_at` - Даты

**Пример:**
```sql
INSERT INTO problems (subject_id, topic_id, problem_id, condition_text, answer, url) 
VALUES (1, 1, '1001', 'Найдите количество...', '5', 'https://math-ege.sdamgia.ru/problem?id=1001');
```

#### 5. `problem_condition_images` - Изображения условий
Хранит локальные пути к изображениям из условий задач

**Поля:**
- `id` - Уникальный идентификатор
- `problem_id` - Связь с задачей
- `image_url` - Оригинальный URL изображения (для справки)
- `image_path` - Локальный путь к изображению (относительно папки server)
- `image_order` - Порядок изображения в условии
- `created_at` - Дата создания

**Примечание:** Изображения сохраняются в `image_tasksdb/{subject}/{problem_id}/condition_0.png`

#### 6. `problem_solution_images` - Изображения решений
Хранит локальные пути к изображениям из решений задач

**Поля:**
- `id` - Уникальный идентификатор
- `problem_id` - Связь с задачей
- `image_url` - Оригинальный URL изображения (для справки)
- `image_path` - Локальный путь к изображению (относительно папки server)
- `image_order` - Порядок изображения в решении
- `created_at` - Дата создания

**Примечание:** Изображения сохраняются в `image_tasksdb/{subject}/{problem_id}/solution_0.png`

#### 7. `category_problems` - Связь задач с категориями
Многие ко многим: задача может принадлежать нескольким категориям

**Поля:**
- `id` - Уникальный идентификатор
- `category_id` - Связь с категорией
- `problem_id` - Связь с задачей
- `created_at` - Дата создания

#### 8. `problem_analogs` - Аналогичные задачи
Хранит связи между аналогичными задачами

**Поля:**
- `id` - Уникальный идентификатор
- `problem_id` - Основная задача
- `analog_problem_id` - Аналогичная задача
- `created_at` - Дата создания

#### 9. `tests` - Тесты
Хранит информацию о тестах из СДАМ ГИА

**Поля:**
- `id` - Уникальный идентификатор
- `subject_id` - Связь с предметом
- `test_id` - ID теста из СДАМ ГИА
- `test_name` - Название теста (если есть)
- `created_at`, `updated_at` - Даты

#### 10. `test_problems` - Задачи в тестах
Связь тестов с задачами

**Поля:**
- `id` - Уникальный идентификатор
- `test_id` - Связь с тестом
- `problem_id` - Связь с задачей
- `problem_order` - Порядок задачи в тесте
- `created_at` - Дата создания

## Иерархия данных

```
Предмет (subject)
  └── Задание (topic)
      └── Категория (category)
          └── Задача (problem)
              ├── Изображения условий (problem_condition_images)
              ├── Изображения решений (problem_solution_images)
              └── Аналогичные задачи (problem_analogs)
```

## Использование

### Инициализация базы данных

```bash
# Через TypeScript скрипт
cd server
npx ts-node database/init-db.ts

# Или напрямую через SQLite
sqlite3 tasksbd.db < database/schema.sql
```

### Примеры запросов

#### Получить все задачи по предмету
```sql
SELECT p.*, s.name as subject_name, t.topic_name
FROM problems p
JOIN subjects s ON p.subject_id = s.id
LEFT JOIN topics t ON p.topic_id = t.id
WHERE s.code = 'math' AND s.exam_type = 'oge';
```

#### Получить задачу с изображениями
```sql
SELECT 
  p.*,
  GROUP_CONCAT(pci.image_path) as condition_images,
  GROUP_CONCAT(psi.image_path) as solution_images
FROM problems p
LEFT JOIN problem_condition_images pci ON p.id = pci.problem_id
LEFT JOIN problem_solution_images psi ON p.id = psi.problem_id
WHERE p.problem_id = '1001' AND p.subject_id = 1
GROUP BY p.id;
```

**Примечание:** Текст задачи (`condition_text` и `solution_text`) уже содержит ссылки на изображения в формате Markdown.

#### Получить задачи по линии
```sql
SELECT p.*
FROM problems p
WHERE p.line = '1';
```

#### Получить задачи из категории
```sql
SELECT p.*
FROM problems p
JOIN category_problems cp ON p.id = cp.problem_id
WHERE cp.category_id = 1;
```

#### Получить аналогичные задачи
```sql
SELECT p2.*
FROM problems p1
JOIN problem_analogs pa ON p1.id = pa.problem_id
JOIN problems p2 ON pa.analog_problem_id = p2.id
WHERE p1.problem_id = '1001';
```

## Индексы

База данных содержит индексы для оптимизации запросов:
- По коду и типу экзамена предметов
- По subject_id и topic_id в задачах
- По problem_id для быстрого поиска
- По category_id и problem_id в связях

## Триггеры

Автоматическое обновление поля `updated_at` при изменении записей в таблицах:
- subjects
- topics
- categories
- problems
- tests

## Миграции

При изменении структуры БД создавайте файлы миграций в формате:
```
database/migrations/YYYYMMDD_HHMMSS_description.sql
```

## Резервное копирование

```bash
# Создать резервную копию
sqlite3 tasksbd.db ".backup backup_$(date +%Y%m%d_%H%M%S).db"

# Восстановить из резервной копии
sqlite3 tasksbd.db < backup_20240101_120000.db
```

## Производительность

- Используйте индексы для частых запросов
- Для больших объемов данных рассмотрите использование PostgreSQL
- Регулярно выполняйте `VACUUM` для оптимизации БД

```sql
VACUUM;
ANALYZE;
```

