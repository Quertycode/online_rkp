# 🔍 Анализ пропущенных данных при импорте из СДАМ ГИА

## ❌ Проблемы, которые были найдены:

### 1. **Связь задачи с категорией НЕ сохраняется**
   - **Проблема:** При импорте задачи через `importProblem()` мы НЕ создаем запись в таблице `category_problems`
   - **Причина:** API `get_problem_by_id()` возвращает только `topic` (номер задания), но не возвращает информацию о категории
   - **Решение:** Нужно либо использовать `get_category_by_id()` для определения категории, либо добавить параметр категории при импорте

### 2. **topic_line из каталога может не сохраняться**
   - **Проблема:** В каталоге может быть поле `topic_line`, но оно не всегда присутствует в структуре данных
   - **Статус:** Частично решено - мы пытаемся сохранить `topic_line` из каталога, но если его нет, то не сохраняется

### 3. **Нет функции для определения категории задачи**
   - **Проблема:** Нет способа автоматически определить, к какой категории относится задача
   - **Решение:** Можно использовать `get_category_by_id()` для каждой категории и проверять, входит ли задача в эту категорию

## ✅ Что сохраняется правильно:

- ✅ ID задачи (`problem_id`)
- ✅ Текст условия (`condition_text`)
- ✅ Текст решения (`solution_text`)
- ✅ Ответ (`answer`)
- ✅ URL задачи (`url`)
- ✅ Номер задания (`topic_id`)
- ✅ Линия заданий (`line`) - если передана или найдена в каталоге
- ✅ Изображения условий (локально)
- ✅ Изображения решений (локально)
- ✅ Аналогичные задачи (`problem_analogs`)
- ✅ Предмет (`subject`)
- ✅ Задание (`topic`)
- ✅ Категории из каталога (`categories`)

## 🔧 Что нужно исправить:

1. Добавить функцию для связывания задачи с категорией
2. Добавить возможность указать категорию при импорте задачи
3. Улучшить определение `topic_line` из каталога


