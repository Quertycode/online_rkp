# 🎵 Система управления музыкой

## 📚 Обзор системы

Реализована полноценная система фоновой lo-fi музыки с возможностью выбора треков через личный кабинет.

---

## 🏗️ Архитектура

### 1. **Плейлист** (`src/assets/music/playlist.js`)
- Содержит список всех доступных треков
- Функции для работы с треками (получение по ID, путь к файлу)
- Легко расширяется новыми треками

### 2. **Хук управления** (`src/hooks/useMusic.js`)
- Глобальный Audio объект (музыка не прерывается при переключении страниц)
- Сохранение выбранного трека в localStorage
- Функция `changeTrack()` для смены трека из настроек

### 3. **Плеер в Header** (`src/components/Header/MusicPlayer.jsx`)
- Кнопка Play/Pause с красивой анимацией
- Индикатор загрузки
- Только управление воспроизведением (без выбора треков)

### 4. **Настройки музыки** (`src/modules/account/components/MusicSettings.jsx`)
- Компонент в личном кабинете
- Список всех треков с радио-кнопками
- Мгновенная смена трека при выборе

### 5. **Интеграция** (`src/pages/Account.jsx`)
- MusicSettings добавлен в личный кабинет
- Отображается под профилем и статистикой

---

## 🎯 Как работает система:

### Для пользователя:
1. **Выбор трека**: Личный кабинет → Блок "Фоновая музыка"
2. **Управление**: Header → Кнопка Play/Pause
3. **Сохранение**: Выбор трека сохраняется автоматически

### Для разработчика (добавление треков):
1. Добавить аудиофайлы в `src/assets/music/`
2. Обновить массив в `src/assets/music/playlist.js`
3. Перезапустить dev-сервер

---

## 📁 Структура файлов

```
src/
├── assets/
│   └── music/
│       ├── playlist.js          ← Список треков
│       ├── README.md            ← Подробная инструкция
│       ├── lofi-track-1.mp3     ← Музыкальные файлы (добавьте сами)
│       ├── lofi-track-2.mp3
│       └── lofi-track-3.mp3
│
├── hooks/
│   └── useMusic.js              ← Хук управления музыкой
│
├── components/
│   └── Header/
│       ├── MusicPlayer.jsx      ← Плеер в Header
│       └── ...
│
├── modules/
│   └── account/
│       └── components/
│           ├── MusicSettings.jsx ← Выбор трека в профиле
│           └── ...
│
└── pages/
    └── Account.jsx              ← Интеграция настроек
```

---

## 🎨 Дизайн компонентов

### MusicPlayer (Header)
- Круглая кнопка с градиентом фиолетовый → розовый
- Иконки Play/Pause SVG
- Spinner при загрузке
- Адаптивный размер

### MusicSettings (Account)
- Карточка с заголовком и иконкой
- Список треков с радио-кнопками
- Подсветка выбранного трека
- Информационная подсказка

---

## 🔧 Технические детали

### Хранение данных:
- **localStorage**: Сохранение выбранного трека
- **Ключ**: `selectedLofiTrack`
- **По умолчанию**: `lofi-1` (первый трек)

### Глобальное состояние:
- **Объект**: `globalAudio` (единственный экземпляр)
- **Зацикливание**: Включено
- **Громкость**: 30%
- **Синхронизация**: Между всеми компонентами

### Обработка событий:
- `playing` → isPlaying = true
- `pause` → isPlaying = false
- `waiting` → isLoading = true
- `canplay` → isLoading = false

---

## 📝 Пример добавления нового трека

### 1. Добавьте файл:
```
src/assets/music/my-new-track.mp3
```

### 2. Обновите playlist.js:
```javascript
export const lofiPlaylist = [
  // ... существующие треки
  {
    id: 'lofi-4',
    name: 'Мой новый трек',
    file: 'my-new-track.mp3',
    duration: '3:30'
  }
]
```

### 3. Готово!
Трек появится в настройках музыки в личном кабинете.

---

## ✅ Преимущества реализации

1. **Модульность**: Каждый компонент отвечает за свою задачу
2. **Масштабируемость**: Легко добавлять новые треки
3. **Сохранение**: Выбор трека сохраняется между сессиями
4. **UX**: Музыка не прерывается при переходах между страницами
5. **Адаптивность**: Работает на всех размерах экранов
6. **Производительность**: Один Audio объект для всего приложения

---

## 🚀 Будущие улучшения (опционально)

- [ ] Регулировка громкости
- [ ] Случайное воспроизведение
- [ ] Плейлисты по настроению
- [ ] Таймер автоотключения
- [ ] Визуализация звука
- [ ] Загрузка треков через админ-панель


